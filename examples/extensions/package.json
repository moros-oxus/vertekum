{
  "name": "@vertekum-example/extensions",
  "private": true,
  "version": "0.3.1",
  "description": "Two extensions holding token data in conformant DTCG files: a minimal custom type, and a derivation with a contributed CLI command.",
  "type": "module",
  "scripts": {
    "check": "vertekum check",
    "build": "vertekum build"
  },
  "devDependencies": {
    "@vertekum/cli": "workspace:*",
    "@vertekum/core": "workspace:*",
    "@vertekum/ext-export-css": "workspace:*"
  }
}
