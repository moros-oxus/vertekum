{
  "name": "@vertekum-example/extensions",
  "private": true,
  "version": "0.3.1",
  "description": "Extensions in action: two DTCG type extensions (config + extension routes) and a generated colour ramp.",
  "type": "module",
  "scripts": {
    "check": "vertekum check",
    "build": "vertekum build"
  },
  "devDependencies": {
    "@vertekum/cli": "workspace:*",
    "@vertekum/core": "workspace:*",
    "@vertekum/ext-export-css": "workspace:*",
    "@vertekum/ext-token-ramp": "workspace:*"
  }
}
