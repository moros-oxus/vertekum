{
  "name": "@vertekum-example/schemas",
  "private": true,
  "version": "0.3.0",
  "description": "Three routes to a token vocabulary: a local schema, an ejected one, and one extending a packaged schema.",
  "type": "module",
  "scripts": {
    "check": "vertekum check",
    "describe": "vertekum describe"
  },
  "devDependencies": {
    "@vertekum/cli": "workspace:*",
    "@vertekum/core": "workspace:*",
    "@vertekum/schema-atlassian": "workspace:*",
    "@vertekum/schema-builder": "workspace:*"
  }
}
