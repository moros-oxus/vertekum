---
"@vertekum/core": minor
---

Scale expressions grow affixes: `evaluateScale` takes optional `prefix`/`suffix` that wrap each generated name around the padded digits while values stay numeric — the shared evaluator now covers t-shirt-style scales (`2xs…4xs`) for schema names and, ahead, token-side materialization. (Coordinated 0.2.0 with the dfn language revision.)
