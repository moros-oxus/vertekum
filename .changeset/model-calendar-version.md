---
"@vertekum/ext-export-figma": minor
---

`figma.model.json` now carries a string contract version (`"version": "draft.01"`) instead of the integer `1`; readers pinned to `1` must accept `draft.01`.
